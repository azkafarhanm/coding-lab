{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Current JS File (WSL)",
      "type": "pwa-node",
      "request": "launch",
      "program": "${file}",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "skipFiles": ["<node_internals>/**"]
    }
  ]
}
